/*

Currency Converter:

Create a program that converts an amount in one currency to another. Prompt the user to enter an amount in one currency (e.g., USD) and a conversion rate to another currency (e.g., EUR). Perform the conversion and display the result.

Allow reverse conversion too.

*/

package main 

import "fmt"




func main()  {

	float32  amount ,result;
	int choice;
     clrscr();
	 fmt.printf("1 for Doller \n")
	 fmt.printf("2 for Euro \n")
	 fmt.printf("3 for Riyal \n")
	 fmt.printf("1 for Dinar \n")
	 fmt.printf("Enter your choice \n")
	 fmt.scanf("%d",choice)
	 fmt.printf("Enter amount in rupees\n")
	 fmt.scanf("%f",&amount)


	fmt.Println
	
}

switch (choice) {
case 1:
result =amount/65
fmt.Printf("%f amount equal to %f", Doller,amount)
case 2;
	result =amount/12;
	fmt.Printf("%f amount equal to %f", Euro,amount);switch (choice) {
		case 3;
		result =amount/69;
		fmt.Printf("%f amount equal to %f", Riyal,amount);switch (choice) {
			case 4;
			result =amount/5;
			fmt.Printf("%f amount equal to %f", Dinar,amount);
	
	default
}













switch (choice) 
case 1;
result =amount/65;
fmt.Printf("%f amount equal to %f", Doller,amount);
switch (choice) 
	case 2;
	result =amount/12;
	fmt.Printf("%f amount equal to %f", Euro,amount);switch (choice) {
		case 3;
		result =amount/69;
		fmt.Printf("%f amount equal to %f", Riyal,amount);switch (choice) {
			case 4;
			result =amount/5;
			fmt.Printf("%f amount equal to %f", Dinar,amount);
	
}